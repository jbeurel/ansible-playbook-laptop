---

apt_repositories:
  - ppa:ansible/ansible

apt_install:
  - openjdk-7-jre
  - vim

  # devlopment
  - ansible
  - nfs-server

  # Web
  - chromium-browser

  # Multimedia
  - vlc

apt_remove:
  # replace by nvm
  - nodejs
